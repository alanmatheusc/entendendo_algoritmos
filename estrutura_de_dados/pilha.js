let arr = new Array(10);
arr.push("thadeu") // puxa o elemento para o final da lista.
arr.pop()  // remove o ultimo elemento da lista.
function isEmpty(arr){
  return arr.length === 0
}

// na pilha também tenho metodos de verificação, o primeiro metodo é verificar se a minha pilha está vazia.

// no javascript não costumama-se criar arranjos com tamanhos fixos, porém em outras linguagens sim, então também tem um metódo para verificar se o arranjo está cheio

